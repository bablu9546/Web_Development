 export default App;
 import { useState } from "react"
  export default function App(){
   const[value,setValue]=useState(0);
   return(
     <div style={{textAlign:'center', marginTop:'20%',}}>
       <h2>{value}</h2>
      <button onClick={(event)=>setValue(value+1)}>click Me</button>

     </div>
   )

 }
